import 'package:flutter/material.dart';

class ColorPages {
  static const COLOR_VERT = Color(0XFF008023);
  static const COLOR_NOIR = Colors.black;
  static const COLOR_BLANC = Color(0XFFFFFFFF);
  static const COLOR_ROUGE = Colors.red;
  static const COLOR_GRIS = Colors.grey;
  static const COLOR_TRANSPARENT=Colors.transparent;
  

}
